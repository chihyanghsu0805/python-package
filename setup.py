"""Setup the package."""
# from distutils.core import setup
from setuptools import setup

"""
setup(
    name="foo",
    version="1.0",
    py_modules=["foo"]
)
"""

"""
setup(
    name="bar",
    version="1.0",
    packages=["bar"],
)
"""
"""
setup(
    name="foo_bar",
    version="1.0",
    py_modules=["foo", "bar.bar"],
)
"""
"""
setup(
    name='foobar',
    version='1.0',
    packages=[''],
)
"""
"""
setup(
    name="foobar",
    version="1.0",
    packages=["foobar"]
)
"""
setup()
